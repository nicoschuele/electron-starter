{
  "name": "electron-starter",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . Electron-Starter --platform=darwin --arch=x64 --version=1.0.0 --overwrite"
  },
  "devDependencies": {
    "electron": "^1.4.5",
    "electron-packager": "^8.2.0"
  },
  "dependencies": {
    "jquery": "^3.1.1"
  }
}
